import React from 'react';
import style from './spinner.module.css';

const Spinner = () => (
    <div className={style.spinner}/>
)

export default Spinner;

//This one is 'borrowed' from https://cssfx.dev/ - both the html (which aint much) and the css (spinner.module.css) (where it shines).